<!--<div class="w3-container">
    <div id="itemsContainer">
    <h1>Thank You<br>
    You have just placed an order <%#= @orderxxx.order_date %></h1>
</div>

<table id="customers">
    <%# @total = 0 %>
    <%# @orderitems.each do |id,quantity| %>
    <tr>
        <%# item = Orderitem.find_by_id(id) %>
        <td><%#= item.title %></td>
        <td><%#= item.quantity %></td>
        <td><%#= number_to_currency(item.price, :unit => "€") %></td> 
        <td><%#= number_to_currency(item.quantity * item.price, :unit => "€") %></td>
    </tr>
    
    <%# timx = (item.quantity * item.price)%>  timx is a variable name which you can call whatever you like 
    <%# @total += timx %>
      
    </tr>
    <%# end %>

    <tr>
        <td>Order Total: &euro; <%#= @total %></td>
    </tr>
</table>
</div>-->


<div class="w3-container w3-padding-32">
    <div class="w3-container w3-quarter"></div>
    <!--<div class="w3-card w3-pale-green w3-border-black w3-half " style="border:6px solid black">-->
    <div class="w3-card w3-border-black w3-half">
        <div class="w3-container w3-white w3-bottombar w3-border-black">
            <div class="w3-panel w3-xxlarge w3-center w3-white">Thanks for your Order</div>
            <div class="w3-panel w3-xxlarge w3-center w3-white">You have just placed an order <%= @orderxxx.order_date %></div>
        </div>
    </div>
  <br>

<!--<div class="w3-container w3-padding-32">
    <div id="itemsContainer">
    <h1>Thank You<br>
    You have just placed an order <%#= @orderxxx.order_date %></h1>
</div>-->

    <div class="w3-container">
        <div class="w3-container w3-center">
            <table id="customers">
                <% @total = 0 %>
                <% @orderitems.each do |id,quantity| %>
                <tr>
                    <% item = Orderitem.find_by_id(id) %>
                    <td><%= item.title %></td>
                    <td><%= item.quantity %></td>
                    <td><%= number_to_currency(item.price, :unit => "€") %></td> 
                    <td><%= number_to_currency(item.quantity * item.price, :unit => "€") %></td>
                </tr>
                <% total = (item.quantity * item.price)%>  #total is a variable name which you can call whatever you like 
                <% @total += total %>
                <!--</tr>-->
                <% end %>
                <!--<tr>
                    <td>Order Total: &euro; <%#= @total %></td>
                </tr>-->
            </table>
        </div>
        <br>
        <div class="w3-cell-row w3-padding">
            <div class="w3-container w3-cell">
                <p></p>
            </div>
        <div class="w3-container w3-white w3-cell w3-border w3-border-black w3-round-xxlarge" style="width:40%">
            <p>Order Total: &euro; <%= @total %></p>
        </div>
        <div class="w3-container w3-cell">
            <p></p>
        </div>
    </div>
    <p> 
        <a href="/items" class="w3-button w3-blue w3-round-xxlarge w3-hover-red">Continue Shopping</a>
        <a href="/clearcart" class="w3-button w3-blue w3-round-xxlarge w3-hover-yellow">Clear Cart</a>
        <a href="/checkout" class="w3-button w3-blue w3-round-xxlarge w3-hover-pink">Confirm Order</a>
    </p>
    </div>
    <div class="w3-container w3-quarter"></div>
</div>